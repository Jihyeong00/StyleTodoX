import * as stylex from '@stylexjs/stylex';

export const S = stylex.create({
  form: {
    width: 400,
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: 'rgba(0,0,0,0.9)',
    padding: 2,
  },
});
